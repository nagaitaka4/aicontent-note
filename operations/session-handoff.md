# セッション引き継ぎ

最終更新：2026-09-09 09:54

---

## 次のセッションでまず行うこと

**TOP10 #1「Claude Code Dynamic Workflowsを、非エンジニアのコンテンツ運用に使ってみた」。**
⚠️ **構成案からではなく、下の1→2→3の順で進める**（2026-09-09にユーザーと確定）。

### 1. まずCCが実際にDynamic Workflowsを回す（ユーザーの作業はゼロ）

**記事の材料は「実際に使った結果」しかない。**解説記事にしないと決めているので、**回す前に構成案を書き始めない。**

**第1候補：記事の目視チェック10項目を並列で回す。**
`operations/article-self-check.py`の出力末尾にある「👁 判断チェック（このスクリプトでは検出不可・目視で毎回確認）」の10項目
（事実以外を書いていないか／AIと人が対等な目線か／一般論と自社サービスの混同／軸の重複／体験談が3割を超えていないか ほか）は、
**いまCCが1項目ずつ目で見ている。**ここを1項目1エージェントで並列に回すと、no.64が「人に残った」と書いた判断のひとつが減るかどうかを直接試せる。

第2候補：既存記事とのカニバリ確認（1記事1エージェントで19本と突き合わせる。2026-09-09は手作業でやった）。

- 起動は`ultracode`キーワード（単発）。⚠️ **`/effort ultracode`はセッション全体に効いてトークンを食うので使わない**
- ⚠️ **サイズの既定は`medium`（15エージェント未満）。**25超か予測150万トークン超で`Large workflow`警告が出る
- **回したら`/workflows`で実測を取る**（エージェント数・トークン・所要時間・どこで詰まったか）。この数字が記事の芯になる
- ⚠️ **うまくいかなくても記事は生きる。**できなかったことがそのまま材料になる（V-05と同じ扱い）

### 2. no.64との距離を決める

**no.64「ブログ運用はどこまで自動化できたか。人に残った2つの判断」（2026-09-02公開）が一番近い。**
no.64が「人に残った2つの判断」で終わっているので、
**その判断が減るのか／減らないのかを、1で実際にやった結果で答える形**なら続編として成立する。
**ここを決めてから構成案に入る。曖昧なまま書き始めない。**

### 3. `article-precheck.py` → 構成案

`python3 operations/article-precheck.py <slug> "<KW>"` を実行してから`rules/article-flow.md`の手順に入る。
⚠️ タイトルには**製品名を入れる**（2026-08-11の全記事分析：製品名ありは1日あたり表示の中央値0.31・なしは0.06）。

---

## 着手前に確認した3点（2026-09-09・CCが実施済み）

ユーザーから「ほんとに記事にするだけの価値はあるか」「他のClaude Code記事とのカニバリはないか」と問われて実施した。

### ① 機能の現状（公式ドキュメントで直読み）

**⚠️ `operations/article-backlog.md`の記載（2026-06-22時点）は古い。両方とも提供範囲が変わっている。**

**Dynamic Workflows**（[公式](https://code.claude.com/docs/en/workflows)）

| 項目 | 内容 |
|---|---|
| プラン | **全有料プランで使える。**Proは`/config`の「Dynamic workflows」行でオンにする |
| 起動 | `ultracode`キーワードで単発／`/effort ultracode`でセッション全体／`/deep-research`が同梱 |
| 上限 | 同時16エージェント・1回1,000エージェント・`parallel()`と`pipeline()`は1回4,096件 |
| コスト警告 | 25エージェント超、または予測150万トークン超で`Large workflow`表示。既定のサイズは`medium`（15未満） |
| 保存 | `/workflows`で`s`を押すと`.claude/workflows/`にコマンドとして保存できる |

**Artifacts**（[公式](https://code.claude.com/docs/en/artifacts)）

| 項目 | 内容 |
|---|---|
| プラン | **Pro・Max・Team・Enterprise。**バックログの「Team・Enterprise向け」は誤り |
| ⚠️ 共有 | **Pro・Maxでは「公開リンク」しか共有手段がない。**組織内限定の共有はTeam/Enterpriseのみ |
| 既定 | 新規は本人だけに見える |
| 操作 | `/artifacts`で一覧・`Ctrl+]`で最新を開く・`/design`でデザインキャンバス |
| 制約 | 1ページ・バックエンドなし・外部リソースは4CDN＋Google Fontsのみ・16MiB上限 |

### ② 検索需要（⚠️ ここが一番重い）

**Dynamic Workflowsの日本語記事は既に8本以上ある。解説では先行できない。**

[Zenn](https://zenn.dev/canly/articles/45da96250c7028)／[Qiita](https://qiita.com/kai_kou/items/fe9b0e65e2252af773c9)／[note](https://note.com/takanashi_ai/n/n8bdec0922164)／[ディアシステム](https://techblog.dearsystem.jp/blog/2026-06-04-01/)／[Uravation](https://uravation.com/media/claude-code-dynamic-workflows-2026/)／[agentpedia](https://agentpedia.codes/ja/blog/claude-opus-4-8-claude-code-workflows)／[lai.so](https://blog.lai.so/handwritten-workflows/)。**公式の日本語ドキュメント**（`code.claude.com/docs/ja/workflows`）まである。

**ただし8本は全部エンジニア向け**（並列オーケストレーション・リファクタリング・コード監査）。
**「非エンジニアのコンテンツ運用に使ったらどうなるか」は誰も書いていない。ここだけが空いている。**

**⚠️ Artifactsは検索が同名の別機能に食われる。**「Claude Artifacts」で日本語上位に出るのは全部**claude.aiの会話パネルの機能**（[JAPAN AI ラボ](https://japan-ai.co.jp/media/7509/)・[マイナビ](https://tenshoku.mynavi.jp/engineer/guide/articles/Z48zkhEAAB8A4ov2)ほか）で、Claude Codeのそれとは別物。

### ③ カニバリ（Claude Code記事19本すべてと突き合わせ）

近いのは3本。**距離が一番近いのはno.64。**

| 記事 | 距離 |
|---|---|
| **no.64 ブログ運用はどこまで自動化できたか（2026-09-02）** | **最も近い。**結論が「人に残った2つの判断」。⚠️ 9/2に出したばかりで、自動化の話が2本続く |
| no.16〜18 Claude Code実践シリーズ（構成・執筆・入稿・2026年4月） | 同じ「どこまで任せられるか」の問い。ただし当時と道具が違う |
| no.19 Claude Codeでも効率化できなかったこと | 「できなかったこと」の枠が重なりうる |

**⚠️ 機能紹介型は当サイトの実測で当たっていない。**Desktop刷新（no.25）4表示／Opus 4.8（no.29）14／Fable 5（no.36）19。
当たったのは課金変更202・レート制限62。**分かれ目は「自分の使い方に影響する変更か」**（2026-08-11の全記事分析）。

---

## この記事の書き方（CCの結論・条件3つ）

1. **2機能を1本にまとめない。Dynamic Workflows 1本に絞る。**Artifactsは同名衝突が重く、Pro/Maxでは公開リンクしか共有手段がないので読者の使いどころが狭い。Artifactsは別ネタとしてバックログに残す
2. **解説記事にしない。**実際にこのメディアの作業に使って、何が変わったかを書く。既存8本が全部エンジニア向けなので、この角度だけが空いている
3. **着手条件は「先に実際に使うこと」。**ただし**CCが自分で動かせる**（ユーザーの作業なし）＝ #4 Cowork・#10 M-14 のように詰まらない

**⚠️ 1と2は構成案の前提。**上の「次のセッションでまず行うこと」の1→2→3の順で進める。
**このセッションのゴールは構成案の提示**だが、1（実際に回す）と2（no.64との距離）を飛ばすと**解説記事にしかならない。**

---

## 直近の判断・保留事項

### M-16をTOP10から外した（2026-09-09・ユーザー判断）

**「ChatGPTを使っている人の95%は、今もGoogleを使っている」は記事にならない。**理由は4つ。

1. **95%の中身が薄い。**[Similarweb公式](https://aisearch.similarweb.com/blog/is-ai-replacing-search/)の原文は`the share of ChatGPT users who also use Google sat at 95% in September 2025...in May 2026, it's still 95%`で、**同じ人がChatGPTのサイトにもgoogle.comにも訪れているかという「重なり」**。Similarweb自身が`overlap measures whether the same people show up in both audiences, not what happens inside any single research session`と限界を書いている。さらに`the report explicitly excludes gen AI API usage, desktop applications, and in-app AI features`＝**ブラウザ経由のみ**
2. **8か月まったく動いていない**（2025年9月も2026年5月も95%）。飽和した指標で変化を語れない
3. **自サイトの答えが1行で終わる。**生成AI機能の表示53に対し通常検索2,580＝**約2%**（`operations/site-status.md` 2026-09-02）
4. **読者とズレる。**このメディアの読者は市場観測を読みに来ていない。加えて受注導線カテゴリは6本で3か月185表示・クリック2

**CC側の反省**：カニバリだけ見て「テーマとしては空いている」と書き、**需要を確かめずに推奨した。**ユーザーの「誰が興味ありますか」で気づいた。

⚠️ **76%→53%（生成AIウェブトラフィックに占めるChatGPTのシェア）と1.6%→6.8%（ChatGPTの回答に引用が出る割合）は生きている数字。**ただしどちらも「Googleは消えない」ではなく逆方向を指す。既存GEO記事のリライト補強に回す。

### 保留：X下書き`ADS63-訂`を出すかどうか

`knowledge/x/ideas.md`の`ADS訂正-判断`。8/24の`ADS-気`に事実誤認が乗っており、CCの推奨は「削除せず訂正ポストを出す」。**下書きは未作成。**ユーザーが「出す」と決めてから`rules/x-post-flow.md`を読み直して作る。

---

## このセッションで完了したこと（2026-09-08〜09）

### no.57リライトを反映（TOP10 #1完了）

`search-console-crawled-not-indexed`／post ID 779。**URLとスラッグは変更なし。**

| 項目 | 内容 |
|---|---|
| タイトル | サーチコンソール「クロール済み - インデックス未登録」で何をしたか → **クロール済み - インデックス未登録の確認方法と、対応の判断基準**（32字） |
| 本文 | 3,680字 → 4,246字。H2を6本に再構成・NG0件 |
| WP反映 | 2026-09-08 14:28（RESTで全文差し替え・SHA-256一致を確認） |
| アイキャッチ | media 922に差し替え。旧2件（781・783）はユーザーが削除済み |
| 再インデックス | 2026-09-08 15:19にユーザーがリクエスト済み |
| 効果検証 | **2026-09-17の定点観測。**前は表示202・クリック0・**74.6位** |

⚠️ **判定は1回で出さない。**効かなかった3施策（no.40・service/・孤立解消）はいずれも「中位で滞留」で、今回は「そもそも答えとして評価されていない」74.6位からの変更。記録は`operations/rewrite-log.md`。

### 公開済み記事のリライトで学んだこと

- **公開済み記事のリライトは、下書きを経由せず本番がそのまま入れ替わる。**「公開ボタンは押さない」が成立しないケース
- **`md-to-wp.py`は`wp:list`を作れない。**WP現行にあったSWELLの番号付きリスト（`swl-pale-04`背景）は、全文差し替え前にブロック数を突き合わせて検出し、手で復元した
- **base64の手写しは1文字化ける。**今回も「インデックス」→「インデッス」が1件。SHA-256照合でのみ検出できた（H2数・マーカー数は一致していた）

---

## X編集部の状態（2026-09-09 08:04時点）

| 項目 | 内容 |
|---|---|
| **投稿TOP1** | `PERM-01`（軸②・★★★★★・auto modeの確認が1週間で2回増えた）。**🚩 鮮度期限〜9/4を5日超過。**`v2.1.261`で3回目が起きているので「1週間で2回」→「3回」に差し替えれば鮮度が戻る。**改稿の判断はX編集部**。⚠️ `CU-気`（9/7投稿）・`AUTOTAB-01`と「許可・画面制御」ゾーンが重なるので3本を連続させない |
| **確定枠** | なし |
| **キュー在庫** | **36本**（9/9 07:25の`GSC-リ`投稿で37→36）。★★★★★は10本前後・軸②比率は目標50%に対しおおむね充足。**取材は不要** |
| **リプライ除外** | @techtalkjp→9/23以降／@Gozengogo_→9/21／@tsuj→9/17／@kgsi→9/16／@lklkfafa1→9/14／@kensuu→9/13／@tsuchinao83→9/11。**解除済**：@cwmasaki（9/9〜）・@kiwi459_index（9/7〜）。**リプライ先そのものは投稿日にリサーチして決める** |
| **直近の投稿** | `GSC-リ`（09/09 07:25・軸③・リンク型・計測待ち）／`CU-気`（09/07・軸②・13ビュー）／`自動化64-リ`（09/03・軸③・8ビュー） |
| **リサーチ連携** | 9/9のデイリーは**キュー投入0本**。②で`ChatGPT Images 2.5`を`ideas.md`へ1件（`research-20260909-01`）。**未処理の旬ネタなし** |
| ⚠️ 整合チェック | 9/9に**新規のズレ3件**を検出。`GSC-リ`の投稿で`RW57-02`・`RW57-01`・`HR-01`が直近5と重なった。**3本とも削除せず、間隔を空ける注記を入れてある** |

順位・下書き本文の正は`knowledge/x/queue.md`。

---

## 今週の期限つき

- **2026-09-11（金）12:00**：`marketing-research-mon-fri`フル版（自動）
- **2026-09-17**：GSC定点観測。**no.57リライトの効果検証**＋no.46リライトの判定（表示117→214で伸びている最中は書き換えない）
