# WPブロック活用ガイド（SWELL）

記事制作時にどのブロックをどう使うかの運用基準。
MDファイル上では`>`（Markdown引用記法）を使わず、`【ブロック名】`を1行目に単独で書く形式で記載する（2026-07-22〜）。WP入稿時に差し替える。

行を分けたい箇所は`<br>`を使う（空行ではなく`<br>`。2026-07-22再訂正）。

```
【ポイント】<br>
内容
```

複数項目を列挙する場合も、各項目の末尾に`<br>`を付けて改行する（最後の項目には不要）。

**この`<br>`ルールは【ブロック名】の中に限らない（2026-07-23〜）**：本文中の`・`箇条書き（ブロックに入っていない、地の文の中の箇条書き）でも、複数項目を並べる場合は空行ではなく`<br>`で改行する。空行区切りは段落として扱われWP上で余白が大きく空くうえ、コピペ後の編集が大変になるため使わない。

```
【インフォ】<br>
見出し<br>
・項目1<br>
・項目2
```

**理由**：
- `>`（Markdown引用）を使うと、WPにコピペした際に各行が個別の引用ブロックとして認識され、ユーザー側で1行ずつ`>`マークを消す作業が発生する
- 単純な改行（空行なし）はMarkdown上で無視され、行が連結されて1行の文章になってしまう
- 空行を入れると別々の段落として扱われ、WP上で段落間の余白が大きく空いてしまう
- `<br>`を使うと、1つの段落のまま・余白を作らずに・行だけを分けられる。この3点を同時に満たすのは`<br>`のみ

---

## 入稿は変換スクリプトで行う（2026-08-26新設・手作業は廃止）

```
python3 operations/md-to-wp.py articles/<slug>.md
```

出力された `operations/wp-output/<slug>.wp.txt` を、WPの記事編集画面 → 右上「⋮」→
**「コードエディター」**に全文貼り付ける（ビジュアルエディタに貼るとブロックにならない）。

これで次が自動で入る。**手で1つずつブロックを作る作業はしない。**

- 【ブロック名】→ SWELLのスタイル付き段落
- 区切り線（各H2直前・CTA直前・記事末尾＝合計7本前後）
- 表・H2・H3・内部リンク・箇条書きの装飾
- CTAボタン（`loos/button`）・関連記事（`loos/post-link`・記事IDは公開APIで自動取得）
- 2文以上の段落の句点改行（`<br>`）

**変換しないもの**（記事ごとに人が判断して付ける装飾）：リード1文目のマーカー、
`is-style-stitch` などの段落装飾、画像・キャプションボックス。

### MDのブロック名とSWELLクラスの対応（2026-08-26に入稿済み61本を実測）

| MD記法 | SWELLクラス | 実測 |
|---|---|---|
| 【ポイント】 | `is-style-big_icon_point` | ○ |
| 【チェック】 | `is-style-big_icon_check` | ○ |
| 【バツ印】 | `is-style-big_icon_batsu` | ○ |
| 【アラート】 | `is-style-big_icon_caution` | ○ |
| 【はてな】 | `is-style-big_icon_hatena` | ○ |
| 【メモ】 | `is-style-big_icon_memo` | ○ |
| 【グッド】 | `is-style-icon_good` | ○ |
| 【インフォ】 | `is-style-icon_info` | ○ |
| 【アナウンス】 | `is-style-icon_announce` | ○ |
| 【バッド】 | `is-style-icon_bad` | 未実測（初回は表示確認） |
| 【ペン】 | `is-style-icon_pen` | 未実測（初回は表示確認） |
| 【本】 | `is-style-icon_book` | 未実測（初回は表示確認） |

**ブロック内の`<strong>`は付けない**（実測では記事ごとにばらついていた。手作業だったためで、
意図的な使い分けではない。スクリプトで素に統一した）。

**`・`箇条書きの装飾は `has-border -border03 is-style-bg_grid`**（実測118件中93件がbg_grid系・
最多65件がこの組み合わせ）。

---

## セクション区切り線（2026-08-20確定・省略禁止）

WP入稿時、**区切り線ブロック（`wp:separator`）を次の3箇所に入れる**。

1. **各H2見出しの直前**（最初のH2の前・CTA直後のH2の前も含む）
2. **CTAブロックの直前**（途中CTA・末尾CTAとも）
3. **記事末尾**（末尾CTAボタンの後に1本）

マークアップは既存記事と同一：

```
<!-- wp:separator -->
<hr class="wp-block-separator has-alpha-channel-opacity"/>
<!-- /wp:separator -->
```

**経緯**：no.55までは各H2の前に区切り線が入っていたが、no.59・no.60で抜けたままユーザー確認まで進み、2026-08-20に指摘された（no.60はユーザー判断でそのまま公開）。パターンはno.55（rule-file-basics）の実測が基準。

---

## ブロック一覧と使いどころ

### 強調・注意系

| ブロック | 色 | 使いどころ | MD記法 |
|---|---|---|---|
| ポイント | オレンジ | 読者に持ち帰ってほしい気づき・要点 | `【ポイント】` |
| チェック | 緑 | うまくいったこと・確認済みの事実・OKな手順 | `【チェック】` |
| バツ印 | 赤 | エラー・失敗・NG例・やってはいけないこと | `【バツ印】` |
| アラート | 黄 | 注意・落とし穴・見落としやすいこと | `【アラート】` |
| はてな | 青 | 疑問・不確かなこと・調査中の事項 | `【はてな】` |
| メモ | グレー | 補足情報・ちょっとした余談 | `【メモ】` |

### 比較・評価系

| ブロック | 使いどころ | MD記法 |
|---|---|---|
| グッド | メリット・良かった点・おすすめ理由 | `【グッド】` |
| バッド | デメリット・悪かった点・注意点 | `【バッド】` |

### 情報・解説系

| ブロック | 使いどころ | MD記法 |
|---|---|---|
| インフォ | 背景情報・補足説明・定義 | `【インフォ】` |
| アナウンス | 更新情報・お知らせ・変更点 | `【アナウンス】` |
| ペン | ライティング・制作に関するTips | `【ペン】` |
| 本 | 参考情報・用語説明・引用元 | `【本】` |

### 会話・引用系

| ブロック | 使いどころ | MD記法 |
|---|---|---|
| 吹き出し | 実際の画面テキスト・会話・セリフ調の引用 | `【吹き出し】` |

**外部からの引用に `>` は使わない**（このファイルの4行目・`rules/article-flow.md` §5・`article-self-check.py`のNG判定と一致させる。2026-08-18に矛盾を解消）。

---

## スタイル一覧と使いどころ

ブロックに追加で装飾を加えるオプション。多用しない。

| スタイル | 使いどころ |
|---|---|
| 付箋 | カジュアルなメモ・ちょっとした補足 |
| 左に縦線 | 重要な一文・印象に残したい言葉 |
| 窪み | 定義・説明ボックス |
| かぎ括弧 | キーワード・短いフレーズを目立たせる |
| 浮き出し | セクションの区切り・まとめボックス |
| ストライプ | 使用頻度低・デザイン的なアクセント用途 |

---

## ボーダー設定

| 番号 | 見た目 | 使いどころ |
|---|---|---|
| 01 | 細線 | シンプルな囲み |
| 02 | 太線・二重 | 重要・強調したい囲み |
| 03 | 点線 | 仮・参考・軽い囲み |
| 04 | 角丸・薄い | ソフトな補足情報 |

---

## SWELLブロック（構造・レイアウト系）

記事の構造を整えるために使う。多用しすぎない。

| ブロック | 使いどころ | MD記法 |
|---|---|---|
| ステップ | 手順を番号付きで見せる（設定・ハウツー系） | `<!-- ステップブロック -->` でメモ |
| FAQ | よくある質問と回答をセットで見せる | `<!-- FAQブロック -->` でメモ |
| アコーディオン | 補足情報・読まなくていい人向けの詳細を折りたたむ | `<!-- アコーディオン -->` でメモ |
| リッチカラム | 2カラム比較（ツール比較・Before/Afterなど） | `<!-- リッチカラム -->` でメモ |
| 説明リスト(DL) | 用語とその説明をセットで並べる | `<!-- 説明リスト -->` でメモ |
| ふきだし | 会話形式・クライアントとのやり取り再現 | `<!-- ふきだし -->` でメモ |
| タブ | 複数のパターンを切り替えて見せる | `<!-- タブブロック -->` でメモ |
| キャプションボックス | 画像に説明を添えるとき | WP入稿時に対応 |
| SWELLボタン | CTA・リンクを目立たせる（SEO記事・仕事記事のみ） | `<!-- SWELLボタン: テキスト / URL -->` でメモ |
| 関連記事 | 記事末尾の関連記事表示 | MDのコメントアウトから設定 |
| リンクリスト | 複数の外部リンクをまとめて見せる | `<!-- リンクリスト -->` でメモ |

---

## 埋め込み（このメディアで使う可能性があるもの）

| ブロック | 使いどころ |
|---|---|
| Twitter埋め込み | X（旧Twitter）の投稿を記事内に表示する。実況感・リアリティの補強に使える |
| YouTube埋め込み | 動画コンテンツを作った場合。現時点では出番少なめ |

その他の埋め込み（Spotify・TikTok等）は現時点では使用予定なし。

---

## パターン（SWELLプリセット）

WP編集画面の「パターン」タブから呼び出せる雛形。

| パターン | 内容 |
|---|---|
| [SWELL] テーブル | 比較表・まとめ表などのテーブルレイアウト |
| [SWELL] ページ用 | 固定ページ向けのレイアウト |
| [SWELL] 汎用パターン | ランディング・セクション区切りなど汎用的なレイアウト |

記事制作では「[SWELL] テーブル」が比較系記事で使いやすい。

---

## 使用頻度の目安

- **多用するブロック**：ポイント・チェック・バツ印
- **ときどき使うブロック**：アラート・グッド・バッド・メモ
- **特定の場面だけ使うブロック**：吹き出し・インフォ・アナウンス・はてな
- **スタイル・ボーダー**：1記事に多くても2〜3か所。使いすぎると見た目がうるさくなる

### ⚠️ ブロック要素を連続で並べない（2026-08-13追加）

**`【】`の強調ブロックは「そこだけ目立たせる」ための箱。3個以上を連続で並べると強調が機能しなくなり、箱が並んだだけの別の壁になる。**手順や項目の列挙は`・`箇条書き（各行末`<br>`）か表で書く。

- **`【ステップ1】`のような記法は存在しない。**`【】`に入れるのは上の一覧にあるブロック名だけ。手順を番号付きで見せたい場合はSWELLの構造ブロックを使い、MDには`<!-- ステップブロック -->`とメモする
- **ステップブロックの用途は「設定・ハウツー系」。**実測の記録・体験の流れを書く記事で使うと、記事の立場（記録）と見た目（マニュアル）が食い違う。記事がHowToかどうかを先に決めてから選ぶ
- 経緯：no.57で`【ステップ1】`〜`【ステップ5】`を並べたが、記法がガイドになく、かつ「HowToにしない」という記事方針と逆の見た目になっていた

---

## 記事種別ごとの傾向

| 記事種別 | よく使うブロック |
|---|---|
| 副業検証シリーズ | バツ印（失敗・エラー）・ポイント（気づき）・メモ（余談） |
| SEO記事 | ポイント・チェック・グッド・バッド（比較）・アラート |
| 仕事記事 | チェック・ポイント・インフォ |
